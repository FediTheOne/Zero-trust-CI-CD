# Web Framework
flask==3.1.3

# Application Server (Fixes Request Smuggling)
gunicorn==22.0.0

# Security & Utilities (Fixes File Overwrite & Credential Leaks)
requests==2.33.0
python-dotenv==1.2.2